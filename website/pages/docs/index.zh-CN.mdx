import { Disclosures } from '../../components/home/faq';
import { Tab, Tabs } from 'nextra-theme-docs';

# 介绍

Million.js 是一个极其快速且轻量级的 `<4kb` 虚拟 DOM, 使 React 速度提高了 [70%](https://krausest.github.io/js-framework-benchmark/current.html)。通过使用经过微调、优化的虚拟 DOM, Million.js 减少了 React 的开销。

想象一下 React 组件以原始 JavaScript 的速度运行。

## 常问问题

<Disclosures full locale={"zh-CN"} />
