---
title: 'renderToTemplate()'
description: 'La fonction renderToTemplate est utilisée pour rendre un nœud de DOM virtuel sous forme de chaîne de caractères. Cela est utilisé pour créer le modèle du bloc et fonctionne de manière tangentielle avec stringToDOM.'
---

import { Callout } from 'nextra-theme-docs';

<Callout type="warning">
  **Cette fonction fait partie de l'API interne.** Elle est destinée aux développeurs qui créent leurs propres frameworks. Il n'est pas recommandé de l'utiliser généralement.
</Callout>

<br />

# `renderToTemplate(){:jsx}`

**Syntaxe:** `renderToTemplate(vnode, edits){:jsx}`\
**Exemple:** `renderToTemplate(<div>Hello World</div>, []){:jsx}`

La fonction `renderToTemplate()` est utilisée pour rendre un nœud de DOM virtuel sous forme de chaîne de caractères. Cela est utilisé pour créer le modèle du bloc et fonctionne de manière tangentielle avec [`stringToDOM(){:jsx}`](/docs/internals/string-to-dom).

```jsx
import { renderToTemplate } from 'million';

const edits = [];
const template = renderToTemplate(<div>Hello World</div>, edits);

console.log(template); // '<div>Hello World</div>'

console.log(edits); // []
```

## Les trous dans les modèles

Vous pouvez également passer des trous à la fonction `renderToTemplate()`. Cela renverra le modèle avec les modifications.

```jsx
import { renderToTemplate } from 'million';

const edits = [];
const hole = { $: 'hole' };
const template = renderToTemplate(<div>Hello {hole}</div>, edits);

console.log(template); // '<div>Hello </div>'

console.log(edits); // [{ type: 'child', index: 0, hole: 'hole' }]
```
