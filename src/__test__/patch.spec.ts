// import { _ } from '../index';
// import { m, style, className } from '../m';
// import { hooks, patchProps, patchChildren, patch } from '../patch';
// import { fireEvent } from '@testing-library/dom';

describe('.patch', () => {
  it('', () => {
    expect(true).toEqual(true);
  });
});
